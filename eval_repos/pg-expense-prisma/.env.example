# Database connection URL for Prisma
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/expense_db"

# Legacy environment variables (for compatibility with original code)
DB_USER=postgres
DB_HOST=localhost
DB_NAME=expense_db
DB_PASSWORD=postgres
DB_PORT=5432